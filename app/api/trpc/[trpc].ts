// // app/api/trpc/[trpc].ts

// import { createTRPCRouter } from "@trpc/server/adapters/next";
// import { appRouter } from "./appRouter"; // Update the path to the appRouter

// export default createTRPCRouter(appRouter);
